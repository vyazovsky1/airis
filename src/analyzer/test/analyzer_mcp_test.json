{
  "_comment": "MCP server config for analyzer tests.",
  "mcpServers": {
    "analyzer": {
      "command": "python",
      "args": ["src/analyzer/mcp_server/analyzer_server.py"],
      "env": {}
    }
  }
}

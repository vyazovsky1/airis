You must output your final decision entirely as a strict, parseable JSON object. 
DO NOT wrap the output in markdown code blocks like ```json ... ```. Just return the raw JSON string.

Your JSON output must map to the following exact structure and rules:
1. "decision": String. This field represents the logical assessment of the resource adequacy.
   - APPROVE: Resources are optimally sized and healthy.
   - COMMENT: Resources are mostly adequate, but minor over-provisioning or optimization opportunities exist.
   - REQUEST_CHANGES: Critical technical issues detected (Inadequate sizing, outage risk, or policy violations).
2. "reasoning": String. Technical explanation of the resource adequacy findings.
3. "target_resources": Nested JSON Object. Contains optimal requested constraints. 
   - VALIDATION RULE: The values for `limits` inside `target_resources` MUST BE PRESERVED exactly as they are in the current `Allocations` context. You are FORBIDDEN from changing limits. 
   - Optimization focus: You MUST optimize the `requests` values based on the analyzed usage pattern.
   - If the assessment is "APPROVE", both `requests` and `limits` in the target block must simply duplicate the original values. 

Expected JSON Structure:
{
  "decision": "YOUR_DECISION_HERE",
  "reasoning": "Detailed explanation...",
  "target_resources": {
    "cpu": {
      "requests": "...",
      "limits": "..."
    },
    "memory": {
      "requests": "...",
      "limits": "..."
    },
    "storage": "..."
  }
}
